import React from 'react'
import { useSelector } from 'react-redux'
const Home = () => {
    // const user = useSelector((state:any)=>{ console.log(state.auth.user); return state.auth.user });
    // console.log(user)
  return (
    <div>Home</div>
  )
}

export default Home